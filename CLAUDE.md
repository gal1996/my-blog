# Claude Code Rules

## Package Manager
- このプロジェクトは **pnpm** を使用します
- npmの代わりに常にpnpmを使ってください

## Commands
- Install dependencies: `pnpm install`
- Run tests: `pnpm test`
- Run dev server: `pnpm dev`
- Build: `pnpm build`
- Lint: `pnpm lint`